# Neon Postgres connection string from Vercel → Storage → your DB → .env.local
DATABASE_URL="postgres://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Optional: secret token used to access /admin to enter actual results
ADMIN_TOKEN="change-me-to-a-long-random-string"
